name: obd_scanner
description: OBD-II diagnostics via ELM327
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: "^3.12.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.1
  flutter_blue_plus: ^1.32.0
  permission_handler: ^11.3.0

flutter:
  uses-material-design: true
